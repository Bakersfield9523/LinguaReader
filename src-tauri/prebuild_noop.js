// Temporary no-op prebuild marker.
// Frontend (dist/) is built manually via `npm run build` before `cargo tauri build`.
console.log("frontend prebuilt via manual npm run build; skipping beforeBuildCommand");
